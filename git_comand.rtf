
Clear - почистити вікно.
pwd  - поточне знаходження.
git init  - створення локального репозиторія.
git status  -  перегляд файлів.
git add .  - добавити в індекс всі файли.
git add /назва файла/  - добавити в індекс один файл.
git commit  -m   "God"  - добавляємо коміт.
git remote add /назва репозиторія/ /URL/ - підключення до віддаленого репозиторія.
git remote -v  перегляд всіх  видалених репозиторіїв. 
git clone /URL/ - Клоновання з віддаленого репозиторія до себе. 
git push /назва репозиторія/ - заливка у віддалений репозиторій.
git fetch - Для отримання даних в свій репозиторія перша команда.
git pull - розпаковує з віддаленого репозиторія після команди fetch
git log - пергляд історії комітів 
git show   /ід коміта/   -  для перегляду що саме робили в даному коміті. 
git  diff - перегляд які зміни були внесені у файл але ще не добавлені в коміт. 
git rm  /назва файла/ - видалити файл з репозиторію
git branch /назва гілки/  - створити гілку локально. 
git branch -v - перегляд всіх гілок.
git checkout  назва /гілки/  - переключитися з одної гілки на іншу.
git branch -d /назва гілки/  - для видалення гілки локально.
git push /назва репозиторію/ --delete /назва гілки/  - щоб видалити гілку на віддаленому сервері
git merge /назва гілки/ - злиття гілок  
git checkout  /ід коміта/ /назва файлу/ - щоб зробити відкат до попереднього стану.
git --version - перевірити чи вже встановлений git.
git config --global user.name /"імя автора"/ - налаштування імя автора
git config --global user.email /імя автора/ - налаштувати емейл автора глобально.
git config --list - перевірка налаштування Git.
mkdir /назва папки/ - створення папки.
echo > /назва файлу/ - створення файлу. 
echo "текс"  >  /назва файлу/   - створення файла з текстом.
echo "текс"  >>  /назва файлу/ - добавляє у файл
ls - перегляд всіх файлів в папці.
ls -la - переглядвсіх файлів і скритих.
cat /назва файлу/ - читання файлу.
rm /назва файлу/ - видалення файлу.
git branch -m /назва гілки/  -  зміна імя гілки.
git remote - перевірка чи підключення до сервера і які.
